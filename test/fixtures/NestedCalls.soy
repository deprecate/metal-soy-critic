{namespace Test}

/**
 * Test
 */
{template .render}
  {@param name: string}
  {@param title: string}
  {@param? optionalInfo: string}

  <div>
    <h1>{$title}</h1>
    <h2>{$name}</h2>

    {if isNonnull(optionalInfo)}
      <p>{$optionalInfo}</p>
    {/if}

    {if isNonnull(optionalInfo)}
      <p>{$name}</p>
    {elseif}
      <p>{$title}</p>
    {else}
      <p>Empty</p>
    {/if}

    {call OtherComponent.render}
      {param foo: 'bar' /}
      {param content kind="html"}
        {call .helper /}
        {call .helper /}
      {/param}
    {/call}

    {call .helper /}
  </div>
{/template}

{template .helper}
  {@param? test: asdasd}
  <div>helper</div>
{/template}
