{namespace Test}

/**
 * Test
 */
{deltemplate Test.First}
  {@param name: string}
  {@param title: string}
  {@param? optionalInfo: string}

  <div>
    <h1>{$title}</h1>
    <h2>{$name}</h2>

    {if isNonnull(optionalInfo)}
      <p>{$optionalInfo}</p>
    {/if}

    {call .helper /}

    {call OtherComponent.render}
      {param foo: 'bar' /}
    {/call}
  </div>
{/deltemplate}

{deltemplate Othernamespace.Second variant="'neato'"}
  {@param name: string}
  {@param title: string}
  {@param? optionalInfo: string}

  <div>
    <h1>{$title}</h1>
    <h2>{$name}</h2>

    {if isNonnull(optionalInfo)}
      <p>{$optionalInfo}</p>
    {/if}

    {call .helper /}

    {call OtherComponent.render}
      {param foo: 'bar' /}
    {/call}
  </div>
{/deltemplate}
